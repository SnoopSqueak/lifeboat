just one makefile in the top level?
that way it can:
        build the units
        build the unit tests
        run the unit tests
        build the integration tests
        run the integration tests
        build the app

scaling code:
        limit folder contents to 3-7 modules
        1 module = 1 c file and 1 h file, usually
        tests live in similarly structured but separate src folder
